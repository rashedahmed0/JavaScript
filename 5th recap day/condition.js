console.log();
let number = 70;
let isJob = true;
let salary = 50000;
let haveHome = true;

if (number >= 33) {
    // console.log('you are passed');
}
else {
    // console.log('you are fail');
}


if (isJob == true && haveHome == true && salary >= 70000) {
    // console.log('you can marry');
}
else {
    // console.log('you can not  marry ');
}

const numericGrade = 84;
const result = (numericGrade >= 40) ? 'pass' : 'fail';
// console.log(result);

const marry = (isJob = true && salary >= 40000 && haveHome) ? 'You can marry' : 'You can not marry  that girl ';
// console.log(marry);


// const userNumber1 = prompt('check your number positive or negative :');

// const check = (userNumber1 < 0) ? `${userNumber1} is a negative number ` : (userNumber1 > 0) ? `${userNumber1} is a positive number ` : 'zero';
// console.log(check);


// let userNumber2 = prompt('check your number even or odd :');
// let parity = (userNumber2 % 2 === 0) ? `${userNumber2} is an even number` : `${userNumber2} is an odd number `
// console.log(parity);

// let entryPassword = 'secret';
// let correctPassword = 'p@ssword';

// let authenticationResult = (entryPassword === correctPassword) ? 'Authentication successful' : 'Authentication failed';
// console.log(authenticationResult);